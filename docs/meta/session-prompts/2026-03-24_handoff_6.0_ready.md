# Handoff — 2026-03-24 — Pre Bloque 6.0 Deploy Producción

## Prompt de Inicio

```
Continuamos el desarrollo de AGENTI / Zentu.

ESTADO ACTUAL:
- Fase activa: 6
- Bloque activo: Bloque 6.0 — Deploy a producción (Vercel)
- Última sesión: 2026-03-24

COMPLETADO EN SESIÓN ANTERIOR:
1. Bug crítico resuelto: entradas de webhook_queue quedaban en `pending` indefinidamente tras
   redespliegues. Root cause: (A) auth check en process-message estaba fuera del try-catch, antes
   de extraer queueId; (B) whatsapp-webhook no actualizaba la cola cuando la invocación fallaba.
   Ambas Edge Functions ya están desplegadas con los fixes en producción (proyecto rutzgbwziinixdrryirv).

2. Polish UI — Home:
   - HomeGreeting: px-5 duplicado eliminado, avatar ahora rounded-full (círculo)
   - HomeNavBar: logo cambiado de /icon.svg → /logo.svg (logo horizontal con texto Zentu, 22px)

3. Rebrand Agenti → Zentu completo:
   - Todas las referencias de usuario ("AGENTI funcionará", "contacto en AGENTI", etc.) → "Zentu"
   - manifest.ts: short_name="Zentu", icon="/icon.svg", purpose="any maskable"
   - layout.tsx: title="Zentu — Tu negocio responde solo", appleWebApp.title="Zentu"
   - SplashScreen: /icon.svg a 88×88px con borderRadius 20px, sessionStorage key "zentu_splash_shown"

OBJETIVO BLOQUE 6.0 — Deploy a producción:
1. Crear proyecto Vercel + conectar repo GitHub
2. Configurar todas las env vars en Vercel Dashboard (.env.local → Vercel env vars):
   - NEXT_PUBLIC_SUPABASE_URL
   - NEXT_PUBLIC_SUPABASE_ANON_KEY
   (Las Edge Functions usan secretos de Supabase, no de Vercel)
3. Deploy en Vercel — verificar build exitoso
4. Actualizar webhook URL de Meta → dominio Vercel (.vercel.app o dominio custom)
5. Smoke test completo en prod:
   - Login/registro
   - Onboarding completo
   - Enviar mensaje WhatsApp → aparece en bandeja
   - QuickInstruct → KnowledgeItem creado
   - Aprobar/rechazar sugerencia
6. Configurar dominio custom si aplica

DECISIONES CRÍTICAS VIGENTES:
- verify_jwt = false en TODAS las Edge Functions (config.toml + --no-verify-jwt en deploy)
- Auth interno con supabase.auth.getUser(jwt) en Edge Functions que requieren usuario autenticado
- process-message, whatsapp-webhook: auth check DENTRO del try-catch, queueId se extrae primero
- Supabase proyecto: rutzgbwziinixdrryirv
- Stack: Next.js 15 App Router, Supabase, TanStack Query v5, Tailwind v4, DM Sans, Solar icons
- Brand color: #3DC185 (token `brand` en Tailwind), logo en /public/logo.svg, icon en /src/app/icon.svg

SKILLS A CARGAR:
- /session-start
- agent-pipeline (si se toca process-message o whatsapp-webhook)
- security (si se tocan env vars, tokens Meta, o configuración de producción)
- supabase-patterns (si se tocan Edge Functions o migraciones)
- error-prevention (siempre al finalizar)
```

---

## Inventario Técnico — Archivos Modificados Esta Sesión

| Archivo | Tipo | Cambio | Impacto |
|---------|------|--------|---------|
| `supabase/functions/process-message/index.ts` | Edge Function | Auth check movido dentro de try-catch; queueId extraído antes del check; auth failure actualiza queue a 'error' | Pipeline robusto: entradas nunca quedan pending sin diagnóstico |
| `supabase/functions/whatsapp-webhook/index.ts` | Edge Function | Invocación de process-message ahora actualiza cola a 'error' si falla; header Authorization explícito | Cierra el segundo gap de pending entries |
| `src/components/dashboard/HomeGreeting.tsx` | Componente | Eliminado px-5 duplicado; avatar rounded-2xl → rounded-full | Alineación correcta al margen del app; avatar circular |
| `src/components/dashboard/HomeNavBar.tsx` | Componente | /icon.svg → /logo.svg a 22px | Logo horizontal con texto en nav bar |
| `.agents/skills/error-prevention/SKILL.md` | Documentación | 2 nuevas entradas: auth-check-outside-try y invocation-no-queue-fallback | Previene reincidencia del bug |
| `.planning/STATE.md` | Estado | Actualizado con sesión 2026-03-24 | Contexto para próxima sesión |

## Archivos de Sesiones Anteriores (contexto acumulado)

| Archivo | Bloque | Estado |
|---------|--------|--------|
| `supabase/functions/process-message/index.ts` | 5.1, 5.3, 6.0 | ACTIVE — pipeline principal |
| `supabase/functions/whatsapp-webhook/index.ts` | 5.1, 6.0 | ACTIVE — entrada de Meta |
| `src/app/manifest.ts` | UI-Zentu | Zentu PWA manifest |
| `src/app/layout.tsx` | UI-Zentu | Title y icons apuntando a Zentu |
| `src/components/SplashScreen.tsx` | UI-Zentu | Splash con /icon.svg |
| `src/components/dashboard/HomeNavBar.tsx` | UI-Zentu, 6.0 | Logo horizontal Zentu |
| `src/components/dashboard/HomeGreeting.tsx` | 6.0 | Alineación y avatar circular |

## Contexto de Producción

- **Supabase proyecto**: `rutzgbwziinixdrryirv`
- **Edge Functions desplegadas hoy**: `process-message`, `whatsapp-webhook`
- **Edge Functions activas (todas)**: `whatsapp-webhook`, `process-message`, `process-quick-instruct`, `confirm-instruction`, `build-agent-context`, `send-message`, `send-notification`, `suggestion-actions`, `generate-daily-summary`, `update-business-profile`
- **App en local**: `http://localhost:3001`
- **Webhook Meta (local ngrok)**: actualizar a dominio Vercel en Bloque 6.0

## Riegos para Bloque 6.0

| Riesgo | Probabilidad | Mitigación |
|--------|-------------|------------|
| Build falla por env vars no configuradas en Vercel | Media | Revisar todas las `NEXT_PUBLIC_*` antes del deploy |
| Webhook Meta sigue apuntando a localhost | Alta | Actualizar URL inmediatamente después del deploy |
| Edge Functions sin secretos en Supabase prod | Media | Verificar `supabase secrets list` antes del smoke test |
| PWA no instala correctamente por manifest | Baja | manifest.ts ya tiene icons con purpose "any maskable" |
