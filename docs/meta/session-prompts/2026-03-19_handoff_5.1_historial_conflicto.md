# Handoff de Sesión — 2026-03-19

## Session Prompt para la siguiente sesión (Copiar y Pegar)

```
Vamos a trabajar en AGENTI. Contexto rápido:

Fase activa: Fase 5 — Entrenamiento, inteligencia y configuración.
Bloque activo: Bloque 5.1 — Instrucción rápida y entrenamiento (cierre pendiente).
Estado: La sesión anterior completó el flujo de conflicto (Reemplazar/Agregar) y M3.3 Historial
de aprendizaje. El DoD del bloque está casi completo.

Pendientes para cerrar el bloque formalmente:
1. Decidir si "Notas de voz" (grabación real) y "Test 1.3 PDFs/imágenes" son necesarios
   para el DoD o se mueven a deuda técnica antes de avanzar a Bloque 5.2.
2. Correr `/phase-done` para verificar el DoD completo del Bloque 5.1.
3. Si se aprueba cierre: avanzar a Bloque 5.2 — Inteligencia y resúmenes.

Decisiones críticas de esta sesión:
1. FLUJO DE CONFLICTO: Cuando una instrucción nueva contradice una existente (mismo topic+layer),
   QuickInstruct muestra un panel ámbar con selector segmentado "Reemplazar / Agregar".
   - Default: Agregar (sin reemplazar la anterior)
   - Reemplazar: desactiva todos los items del mismo topic+layer antes de crear el nuevo
   - El parámetro `replace_previous: boolean` se pasa a confirm-instruction
2. REALTIME + knowledge_count: El handler de Realtime en agent/page.tsx ahora también fetchea
   el knowledge_count al recibir UPDATE en competency_topics, para que el estado binary de
   las cards (gris ↔ verde) actualice en tiempo real.
3. loadTopics PRESERVA items: Al recargar topics (onSuccess), los items pre-cargados de cards
   expandidas se preservan. handleTopicClick siempre refetchea (datos frescos garantizados).
4. M3.3 IMPLEMENTADO: Historial con filtro por tipo, labels traducidos, modal de confirmación,
   reactivar sin modal, llama refresh_competency_coverage tras toggle.
5. SUPABASE PROJECT REF: rutzgbwziinixdrryirv (confirmar antes de deploy).

Ejecuta /session-start para cargar el contexto completo y presenta el plan.
```

---

## Inventario Técnico

**Archivos modificados:**
- `src/components/dashboard/QuickInstruct.tsx`
  - `ConflictingItem` interface nueva
  - State: `conflictingItems`, `replaceMode`
  - Detección de conflicto post-propuesta (query a `competency_topics` + `knowledge_items`)
  - Panel ámbar con selector segmentado "Reemplazar / Agregar"
  - `reset()` limpia conflictingItems y replaceMode
  - `confirmInstruction` pasa `replace_previous: replaceMode`
  - Imports: `RefreshCw`, `PlusCircle` de lucide

- `supabase/functions/confirm-instruction/index.ts`
  - Acepta `replace_previous = false` del body
  - Paso 5: si `replace_previous && topic`, hace UPDATE `active=false` en items del mismo topic+layer
  - Llama `refresh_competency_coverage` via RPC tras persistir
  - **DEPLOYADA** en proyecto rutzgbwziinixdrryirv

- `src/app/dashboard/agent/page.tsx`
  - `loadTopics`: preserva `items` de topics existentes con `prev.find(...)`
  - `handleTopicClick`: eliminado guard `topic.items !== undefined` → siempre refetchea
  - Realtime handler: ahora `async`, fetchea `knowledge_count` fresco para el topic actualizado

- `src/app/dashboard/agent/history/page.tsx` (**REESCRITURA COMPLETA**)
  - `HistoryItem` interface con `source_type`, `active`
  - `LAYER_LABELS` record para traducción de capas
  - `FilterType` + `FILTER_OPTIONS` para filtro segmentado
  - `confirmItem` state para modal de confirmación
  - `requestToggle`: desactivar → modal; reactivar → directo
  - `executeToggle`: toggle + RPC `refresh_competency_coverage` + delete `agent_context_cache`
  - `TypeIcon` component con soporte: quick_instruct, text, voice_note, voice, image_ocr, image
  - Modal de confirmación con preview del contenido

**Documentación actualizada:**
- `docs/event-map.md`: evento `instruction_confirmed` — replace_previous, flujo de conflicto, Realtime
- `docs/module-map.md`: sección M3.3 — funcionalidades reales implementadas

---

## DoD Bloque 5.1

- [x] QuickInstruct funcional end-to-end
- [x] Topics no se duplican
- [x] Coverage actualiza al confirmar
- [x] Realtime en competency_topics
- [x] Áreas del negocio con modelo binario
- [x] Panel expandido con instrucciones
- [x] Instrucción contradictoria: Reemplazar/Agregar
- [x] M3.3 Historial de aprendizaje completo
- [x] Revertir desde historial refleja en mapa en tiempo real
- [ ] Notas de voz (grabación real) — pendiente de decisión
- [ ] Test 1.3 PDFs/imágenes — pendiente de decisión

**Commit:** (ver git log)

---

*Sesión cerrada 2026-03-19*
